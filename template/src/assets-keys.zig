pub const TextureKey = enum {
    spritesheet,
    bg,
};

pub const ImageKey = TextureKey;

pub const SoundKey = enum {
    example,
};

pub const MusicKey = enum {
    example,
};

pub const ShaderKey = enum {
    crt,
    god_rays,
    sobel,
};
